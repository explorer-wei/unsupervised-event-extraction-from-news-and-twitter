package constants;

import java.io.File;

public class GeneralConstants {
	public static final String WORKING_DIRECTORY = "." + File.separator + "workspace";
	public static final String WORD2VEC_MODEL_PATH = "." + File.separator + "models" + File.separator + "uc-vectors.bin";
}
